# Placeholder

Refer to the repository-level AGENTS.md for contribution rules and environment policies. All work happens inside the shared devcontainer—please do **not** create local Python virtual environments in `tests/`; rely on the repo-managed interpreter and the packages declared in `tools/environment_manifest.json`.
